name: Sync docs to palabre-web

on:
  push:
    branches: [main]
    paths:
      - 'docs/guide/fr/**'
      - 'docs/guide/en/**'
      - 'scripts/sync_docs.py'
      - 'scripts/sync_changelog.mjs'
      - 'CHANGELOG.md'
      - '.github/workflows/sync-docs.yml'

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Process docs
        run: |
          python3 scripts/sync_docs.py
          node scripts/sync_changelog.mjs

      - name: Clone palabre-web
        env:
          TOKEN: ${{ secrets.DOCS_REPO_TOKEN }}
        run: |
          git clone https://x-access-token:${TOKEN}@github.com/JuReyms/palabre-web.git palabre-web
          cd palabre-web && git checkout dev

      - name: Copy files
        run: |
          rm -f palabre-web/content/fr/1.get-started/[0-9].*.md palabre-web/content/fr/2.agents/[0-9].*.md palabre-web/content/fr/3.usage/[0-9].*.md palabre-web/content/fr/4.configuration/[0-9].*.md palabre-web/content/fr/5.integrations/[0-9].*.md palabre-web/content/fr/5.reference/[0-9].*.md palabre-web/content/fr/6.reference/[0-9].*.md
          rm -f palabre-web/content/en/1.get-started/[0-9].*.md palabre-web/content/en/2.agents/[0-9].*.md palabre-web/content/en/3.usage/[0-9].*.md palabre-web/content/en/4.configuration/[0-9].*.md palabre-web/content/en/5.integrations/[0-9].*.md palabre-web/content/en/5.reference/[0-9].*.md palabre-web/content/en/6.reference/[0-9].*.md
          rm -f palabre-web/content/fr/6.troubleshooting.md palabre-web/content/fr/7.troubleshooting.md palabre-web/content/fr/7.roadmap.md palabre-web/content/fr/8.roadmap.md palabre-web/content/fr/8.changelog.md palabre-web/content/fr/9.changelog.md
          rm -f palabre-web/content/en/6.troubleshooting.md palabre-web/content/en/7.troubleshooting.md palabre-web/content/en/7.roadmap.md palabre-web/content/en/8.roadmap.md palabre-web/content/en/8.changelog.md palabre-web/content/en/9.changelog.md
          cp -R dist/content/. palabre-web/content/

      - name: Commit and push
        working-directory: palabre-web
        env:
          TOKEN: ${{ secrets.DOCS_REPO_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add content/
          if git diff --staged --quiet; then
            echo "No changes to sync"
          else
            COMMIT=$(git -C .. rev-parse --short HEAD)
            git commit -m "docs : synchroniser depuis Palabre@${COMMIT}"
            git push origin dev
          fi
